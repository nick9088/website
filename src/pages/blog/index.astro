---
import Layout from "../../layouts/Layout.astro";
const allPosts = await Astro.glob('./*.md');
---

<Layout title="blog - nick909">
    <main class="animate__animated animate__fadeIn">
		<h1 class="font-bold text-3xl py-3">blog</h1>
        <p class="font-bold text-xl py-3">work in progress 😊</p>
        <!--<ul class="postlist text-xl">
            {allPosts.map((post) => <li><a href={post.url} class="relative py-1 after:absolute after:bottom-0 after:left-0 after:w-full after:scale-x-0 hover:after:scale-x-100 after:transition-all after:origin-left after:h-[2px] after:bg-mauve">{post.frontmatter.title}</a></li>)}
        </ul>--->
	</main>
</Layout>

<style>
	@import url("https://fonts.googleapis.com/css2?family=Unbounded:wght@200..900&display=swap");
	body {
		@apply flex h-screen;
		padding: 0 1.5rem 1.5rem 1.5rem;
		font-family: "Unbounded", sans-serif;
	}

	.underline-2 {
		border-bottom: 1px solid;
	}

	.favlist {
		list-style: inside;
	}

    .postlist {
        list-style: inside;
    }
</style>